# 安全说明（Security）

## 报告漏洞

如果你发现安全漏洞，请**不要**在公开 Issue 中提交细节（尤其涉及本机路径、日志内容或配置文件时）。

请使用 GitHub 仓库的 **Security → Report a vulnerability**（私密安全公告）功能报告，或先开一个不含敏感细节的占位 Issue 等待维护者联系。

## 支持的版本

| 版本 | 支持状态 |
| --- | --- |
| 最新 Release（0.4.1） | ✅ |
| 更早版本 | ❌ 请升级到最新版 |

## 安装包完整性

安装包未使用商业代码签名证书，首次运行可能触发 Windows SmartScreen 警告。请在运行前用 Release 页面公布的 SHA-256 校验文件完整性，并只从本仓库的 GitHub Release 下载。

## 已知安全限制

以下是当前版本已知的安全限制，供你评估：

- **本地事件端点无认证**：事件服务只绑定 `127.0.0.1:7878`，无法被外部网络访问，但本机其他进程理论上可以伪造事件（影响仅限本地显示状态）。后续版本计划增加本地令牌校验。
- **会修改其他工具的配置文件**：首次启动会写入 Codex、Claude Code、OpenCode、ZCode 的 Hook/插件配置（修改前生成备份）。完整清单见 [PRIVACY.md](PRIVACY.md)。
- **依赖本地文件格式**：监测逻辑读取各工具的本地数据库/日志，若上游格式变化可能失效；相关解析已尽量做防御性处理，会话名称在 UI 中按纯文本渲染以防注入。
