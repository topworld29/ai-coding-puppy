# Changelog

本项目的早期开发未使用 Git，以下条目基于留存的发布说明与验证记录整理。0.3.13 是首个进入本仓库的版本基线；更早版本不编造具体日期与提交历史。

## [0.4.0] - 2026-09-05

### 修复

- **修复点击小狗弹不出面板**：点击/拖动的判定阈值从 3 CSS 像素放宽到 6 像素（`CLICK_SLOP`）。在高 DPI 缩放屏（本机 1.75x）上 3 CSS 像素仅约 5 个物理像素，真人点击按下到抬起的自然漂移就会超过，点击被误判为拖动——面板弹不出且触发窗口拖动。定位手段：document.title 标记 + UIA 读标题的分支追踪法，实测注入 6 物理像素漂移的真实点击稳定复现（togglePanel 不执行）；放宽后同场景稳定打开面板。正常拖动（位移远大于 6px）不受影响。

### 已知问题（待办）

- 面板内的静音/清除/跳转按钮偶发点击无效：面板每秒整秒重建 `innerHTML`，点击的按下与抬起恰好跨过重建瞬间时 `click` 事件不触发（约 1/10 概率）。后续可改为增量更新或 pointer 判定。本版未改动。

## [0.3.22] - 2026-09-05

### 新增

- **托盘菜单顶部显示版本号**（如「小狗桌宠 v0.3.22」）：`enabled=false` 的菜单项，灰色不可点击，仅供查看；版本号编译期取自 Cargo.toml，与五处版本同步单源一致。原有「显示 / 隐藏」「开机自启」「退出」三项不变。

## [0.3.21] - 2026-09-04

### 修复

- **面板与小狗的距离改回 0.3.19 的固定值**：0.3.20 双窗口重构后面板在 panel 窗口内顶边锚定（top:4），内容少时面板底边悬空、距小狗最远超 100px 且随会话数量变化；现改回底边锚定（bottom:4），面板底边到小狗的可见距离恒定（不随会话数量变化），并收紧窗口间隙（PANEL_GAP 12→8），实测可见间隙与 0.3.19 单窗口版一致。
- **移除小狗的焦点轮廓**（键盘导航时的 #e2bd80 描边框）：0.3.17 的键盘门控（keydown 加类/pointerdown 移除类）仍存在无法覆盖的间歇路径，用户偶尔点击小狗仍见描边框；桌宠窗口实际没有键盘可达入口（必须先点击它），描边的使用场景趋近于零，故整段移除（含 CSS 轮廓样式与 keyboard-nav 门控逻辑），aria 无障碍属性保留。Enter/空格开合面板的键盘操作不受影响。

## [0.3.20] - 2026-09-02

### 变更：双窗口固定尺寸架构（恢复点击穿透，不触发透明 bug）

- 解决 0.3.19 的已知取舍（固定 280×240 窗口拦截小狗周边点击）：改为**两个固定尺寸窗口**——pet 窗口只含小狗（56×56，面板收起时周边点击直接穿透到下层窗口）；会话面板迁移到独立 panel 窗口（268×148，默认隐藏，点击小狗时定位并显示/隐藏）。
- 关键安全约束（依据 0.3.18/0.3.19 的透明失效教训）：**两个窗口运行时永不 resize**，面板开关仅做 show/hide 与位置变更（setPosition，历史上安全，与拖动同类），不触碰任何背景色 API。
- 面板打开时拖动小狗会自动收起面板（独立窗口不跟随拖动）；面板清除最后一个会话或跳转窗口成功后自行收起，并通过事件同步小狗的 aria 状态。
- 已知取舍：面板展开时，panel 窗口矩形（268×148 CSS）内的透明部分仍会拦截点击；小狗拖到屏幕上边缘时面板可能在屏幕外（与旧版同级）。

## [0.3.20] - 2026-09-03

### 变更：双窗口固定尺寸架构（恢复点击穿透，不触发透明 bug）

- 解决 0.3.19 的已知取舍（固定 280×240 窗口拦截小狗周边点击）：改为**两个固定尺寸窗口**——pet 窗口只含小狗（56×56，面板收起时周边点击直接穿透到下层窗口）；会话面板迁移到独立 panel 窗口（268×148，默认隐藏，点击小狗时定位并显示/隐藏）。
- 关键安全约束（依据 0.3.18/0.3.19 透明失效教训）：两个窗口运行时永不 resize，面板开关仅做 show/hide 与 setPosition（与拖动同类，安全），不触碰背景色 API。
- 启动时的一次性尺寸归一化（绕过 Windows 创建期 136×39 最小尺寸钳制）必须在页面首帧绘制完成后执行——初始加载期间 resize 会导致 WebView2 渲染面脱离（内容不显示但透明正常），该时序问题已在本版修复并实测验证。
- 两窗口设置 `maximizable/minimizable: false` 防意外最大化；面板打开时拖动小狗自动收起面板；面板自收起通过事件同步 aria。
- 已知取舍：面板展开时 panel 窗口矩形（268×148）内透明部分仍拦截点击；小狗拖到屏幕上边缘时面板可能在屏幕外。

## [0.3.19] - 2026-09-02

### 回退

- **回退 0.3.16 引入的窗口动态收缩**，恢复固定 280×240 窗口。动态 setSize 在本机 WebView2 运行时上不可靠地导致透明背景丢失（窗口非小狗区域渲染为纯白 #FFFFFF，实测像素精确闭合），且 0.3.18 的周期性重应用透明背景补丁疑似同样会触发该问题（新启动实例即白）。固定窗口是产品上线以来透明表现最稳定的架构。
- **回退 0.3.18 的 webview 背景重应用补丁**与相关权限。
- **保留**已验证有效的修复：WebView 右键菜单屏蔽与左键过滤（0.3.15）、键盘操作才显示焦点轮廓（0.3.17）。
- 已知取舍：恢复固定窗口后，小狗周边 280×240 矩形内的左键/右键会被桌宠窗口拦截（不会弹菜单，但不作用于下层窗口）。点击穿透需求留待后续以"双窗口固定尺寸"等不触发 resize 的架构重新实现。

## [0.3.18] - 2026-09-01

### 修复

- 修复点击/右键小狗时出现白色圆角框的问题：窗口动态收缩（0.3.16）触发 WebView2 在尺寸变化时的陈旧合成层残留，旧窗口区域以默认背景色（近白 F8F8F8）渲染，看起来是一个包住小狗的白色圆角容器（Windows 11 自动圆角）。现在通过 `backgroundColor: #00000000` 显式将 WebView2 背景设为全透明，消除残留白底。
- 该问题自 0.3.16 引入、0.3.17 未打中根因（0.3.17 修的焦点描边本身正确但只是叠加症状之一）。

## [0.3.17] - 2026-09-01

### 修复

- 点击/右键小狗不再出现焦点描边框：canvas 非原生可聚焦控件，Chromium 会在鼠标点击聚焦时也命中 `:focus-visible`，此前会显示一圈金褐色轮廓（窗口收缩到 56×56 后紧贴窗口边缘、非常显眼）。
- 焦点轮廓仍保留给真正的键盘操作（Tab 导航等），可达性不受影响。

## [0.3.16] - 2026-08-31

### 变更

- 窗口改为动态收缩，解决透明窗口阻挡周边点击的问题：面板收起时窗口缩小为只包住小狗（56×56 CSS 像素，含 4px 边距以保留键盘焦点轮廓），面板展开时按面板实际高度扩展；切换以窗口右下角为锚点，小狗在屏幕上的位置保持不变。
- 收起状态下，小狗周边（原 280×240 矩形）的左键/右键不再被桌宠窗口拦截，直接作用于下层窗口；右键小狗本体仍无任何菜单（0.3.15 的屏蔽保持不变）。

## [0.3.15] - 2026-08-31

### 修复

- 彻底屏蔽桌宠窗口内的 WebView 默认右键菜单：此前在窗口矩形范围内（包括小狗周围的透明区域）右键会弹出 WebView2 的「返回/刷新/另存为」页面菜单，点在小狗 canvas 上会弹出「将图像另存为/复制图像」图片菜单，导致桌宠周边无法正常使用右键。
- 右键（非左键）不再触发面板开关：`pointerdown` 现在只响应主按键。

## [0.3.14] - 2026-08-31

### 变更

- 项目更名为「小狗桌宠」（英文 Puppy Desktop Pet，原「小金毛桌宠 / Golden Puppy Pet」），产品名、窗口标题、托盘与通知文案、README 等全部文档同步更新。
- 更名后的首个安装包：安装目录由 `%LOCALAPPDATA%\小金毛桌宠` 变更为 `%LOCALAPPDATA%\小狗桌宠`。**从旧版本升级请先卸载旧版再安装本版**；首次启动会自动把各工具的 Hook 配置更新到新安装路径。

## [0.3.13] - 2026-08-31

首个 GitHub 发布版本。

### 修复

- 修复 Codex 与 ZCode 桌面端在用户回答问题后，仍可能持续显示红色「等待输入」的状态粘连：监测器现在区分数据库直接检测到的等待与 Hook 上报的等待，任务恢复运行时及时回到「运行中」。
- 修复小狗本体无法通过键盘操作的问题：支持 `Tab` 聚焦、`Enter` / `Space` 开关会话面板，并补齐 `role` / `aria-label` / `aria-expanded` 等可访问性属性。
- 清理 Rust 静态检查（Clippy）警告。

### 验证

- 280×240 真实窗口尺寸下覆盖空态、2 会话、10 会话滚动布局。
- 覆盖长名称、换行、Emoji、HTML-like 文本、等待/完成/错误状态、静音、清除与窗口跳转。
- Rust 全量测试（25 项）、Clippy、TypeScript 类型检查与生产构建全部通过。

## [0.3.12]

- ZCode 桌面端新增 app-server JSONL 日志增量监测，`turn.started` / `turn.completed` / `turn.failed` 事件更及时；未写完的最后一行不会被提前消费。
- 完成一轮完整 Dogfood / Bug Hunt 验证（多会话布局、特殊字符、键盘可达性等）。

## 0.3.5 – 0.3.11（中间迭代）

此期间的改进未逐一对应版本号，主要包括：

- 新增 ZCode 支持：Hook、SQLite 数据库轮询、桌面日志监测三层兜底。
- 多会话面板固定 264px 宽度、内部滚动不挤压窗口，长会话名单行省略。
- 会话静音功能并持久化到 `muted-sessions.json`，新活动到来前不再打扰。
- 跳转窗口时保持目标窗口原有最大化状态；支持 CLI 嵌套终端进程与各桌面窗口的匹配。
- 小狗形象从全身方案迭代为 16×16 正面头部徽标（当前最终版）。
- 修复 Claude 普通 idle 提示被误判为「等待输入」的问题，并添加回归测试。

## [0.3.4] - 2026-08-29

首个带正式发布说明的版本。

- 支持 Codex、Claude Code、OpenCode 的 CLI 与桌面版会话状态监测。
- 首次启动自动安装/更新各工具桥接：只管理自己的条目、保留已有 Hook，修改现有 JSON 前生成 `.golden-puppy.bak` 备份。
- Windows x64 NSIS 安装包，未签名，随包公布 SHA-256。
