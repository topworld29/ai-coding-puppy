# Changelog

本项目的早期开发未使用 Git，以下条目基于留存的发布说明与验证记录整理。0.3.13 是首个进入本仓库的版本基线；更早版本不编造具体日期与提交历史。

## [0.3.18] - 2026-09-01

### 修复

- 修复点击/右键小狗时出现白色圆角框的问题：窗口动态收缩（0.3.16）触发 WebView2 在尺寸变化时的陈旧合成层残留，旧窗口区域以默认背景色（近白 F8F8F8）渲染，看起来是一个包住小狗的白色圆角容器（Windows 11 自动圆角）。现在通过 `backgroundColor: #00000000` 显式将 WebView2 背景设为全透明，消除残留白底。
- 该问题自 0.3.16 引入、0.3.17 未打中根因（0.3.17 修的焦点描边本身正确但只是叠加症状之一）。

## [0.3.17] - 2026-09-01

### 修复

- 点击/右键小狗不再出现焦点描边框：canvas 非原生可聚焦控件，Chromium 会在鼠标点击聚焦时也命中 `:focus-visible`，此前会显示一圈金褐色轮廓（窗口收缩到 56×56 后紧贴窗口边缘、非常显眼）。
- 焦点轮廓仍保留给真正的键盘操作（Tab 导航等），可达性不受影响。

## [0.3.16] - 2026-08-31

### 变更

- 窗口改为动态收缩，解决透明窗口阻挡周边点击的问题：面板收起时窗口缩小为只包住小狗（56×56 CSS 像素，含 4px 边距以保留键盘焦点轮廓），面板展开时按面板实际高度扩展；切换以窗口右下角为锚点，小狗在屏幕上的位置保持不变。
- 收起状态下，小狗周边（原 280×240 矩形）的左键/右键不再被桌宠窗口拦截，直接作用于下层窗口；右键小狗本体仍无任何菜单（0.3.15 的屏蔽保持不变）。

## [0.3.15] - 2026-08-31

### 修复

- 彻底屏蔽桌宠窗口内的 WebView 默认右键菜单：此前在窗口矩形范围内（包括小狗周围的透明区域）右键会弹出 WebView2 的「返回/刷新/另存为」页面菜单，点在小狗 canvas 上会弹出「将图像另存为/复制图像」图片菜单，导致桌宠周边无法正常使用右键。
- 右键（非左键）不再触发面板开关：`pointerdown` 现在只响应主按键。

## [0.3.14] - 2026-08-31

### 变更

- 项目更名为「小狗桌宠」（英文 Puppy Desktop Pet，原「小金毛桌宠 / Golden Puppy Pet」），产品名、窗口标题、托盘与通知文案、README 等全部文档同步更新。
- 更名后的首个安装包：安装目录由 `%LOCALAPPDATA%\小金毛桌宠` 变更为 `%LOCALAPPDATA%\小狗桌宠`。**从旧版本升级请先卸载旧版再安装本版**；首次启动会自动把各工具的 Hook 配置更新到新安装路径。

## [0.3.13] - 2026-08-31

首个 GitHub 发布版本。

### 修复

- 修复 Codex 与 ZCode 桌面端在用户回答问题后，仍可能持续显示红色「等待输入」的状态粘连：监测器现在区分数据库直接检测到的等待与 Hook 上报的等待，任务恢复运行时及时回到「运行中」。
- 修复小狗本体无法通过键盘操作的问题：支持 `Tab` 聚焦、`Enter` / `Space` 开关会话面板，并补齐 `role` / `aria-label` / `aria-expanded` 等可访问性属性。
- 清理 Rust 静态检查（Clippy）警告。

### 验证

- 280×240 真实窗口尺寸下覆盖空态、2 会话、10 会话滚动布局。
- 覆盖长名称、换行、Emoji、HTML-like 文本、等待/完成/错误状态、静音、清除与窗口跳转。
- Rust 全量测试（25 项）、Clippy、TypeScript 类型检查与生产构建全部通过。

## [0.3.12]

- ZCode 桌面端新增 app-server JSONL 日志增量监测，`turn.started` / `turn.completed` / `turn.failed` 事件更及时；未写完的最后一行不会被提前消费。
- 完成一轮完整 Dogfood / Bug Hunt 验证（多会话布局、特殊字符、键盘可达性等）。

## 0.3.5 – 0.3.11（中间迭代）

此期间的改进未逐一对应版本号，主要包括：

- 新增 ZCode 支持：Hook、SQLite 数据库轮询、桌面日志监测三层兜底。
- 多会话面板固定 264px 宽度、内部滚动不挤压窗口，长会话名单行省略。
- 会话静音功能并持久化到 `muted-sessions.json`，新活动到来前不再打扰。
- 跳转窗口时保持目标窗口原有最大化状态；支持 CLI 嵌套终端进程与各桌面窗口的匹配。
- 小狗形象从全身方案迭代为 16×16 正面头部徽标（当前最终版）。
- 修复 Claude 普通 idle 提示被误判为「等待输入」的问题，并添加回归测试。

## [0.3.4] - 2026-08-29

首个带正式发布说明的版本。

- 支持 Codex、Claude Code、OpenCode 的 CLI 与桌面版会话状态监测。
- 首次启动自动安装/更新各工具桥接：只管理自己的条目、保留已有 Hook，修改现有 JSON 前生成 `.golden-puppy.bak` 备份。
- Windows x64 NSIS 安装包，未签名，随包公布 SHA-256。
